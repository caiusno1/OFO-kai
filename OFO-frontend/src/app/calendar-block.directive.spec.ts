import { CalendarBlockDirective } from './calendar-block.directive';

describe('CalendarBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarBlockDirective();
    expect(directive).toBeTruthy();
  });
});
