import { Participants2ViewPipe } from './participants2-view.pipe';

describe('Participants2ViewPipe', () => {
  it('create an instance', () => {
    const pipe = new Participants2ViewPipe();
    expect(pipe).toBeTruthy();
  });
});
